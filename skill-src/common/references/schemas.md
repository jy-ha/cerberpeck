# Output Rules

Return only the JSON object required by the Action's supplied JSON Schema. Cite artifact-relative paths and visible regions when making findings. Never include secrets, absolute source workspace paths, Champion/Challenger identity, or private A/B mappings in reviewer output.
